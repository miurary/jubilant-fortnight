{{>header}}

<main class="content">
    <aside class="category-container">
        <h2>Sports Category</h2>
        <div class="category-option">

          {{#each sports}}
            {{> sport}}
          {{/each}}

        </div>
</main>
